'use strict';

/**
 * Amount of wizards on the page
 * @const {number}
 */
var WIZARDS_AMOUNT = 4;

window.renderSimilarWizards(WIZARDS_AMOUNT);
window.utils.toggleHidden(document.querySelector('.setup-similar'), false);
